# Markdown linting configuration
default: true

# Line length - disable for code blocks
MD013: false

# Allow inline HTML (useful for badges, etc.)
MD033: false

# Allow duplicate headers (common in READMEs)
MD024:
  siblings_only: true

# First line doesn't need to be h1
MD041: false
